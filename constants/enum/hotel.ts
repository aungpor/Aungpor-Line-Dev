export enum B2BHOTEL_STATUS {
  create = "create",
  approved = "approved",
  confirm = "confirm",
  cancelling = "cancelling",
  cancelled = "cancelled",
  notApproved = "not approved",
}

export enum B2BHOTEL_MODE {
  admin = "a",
  user = "u",
}
