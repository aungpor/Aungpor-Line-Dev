export interface IMission {
  mission_id: string;
  mission_code: string;
  mission_name: string;
  mission_desc: string;
  limit_promo_per_giver: number;
  limit_promo_per_receiver: number;
  limit_promo_per_mission: number;
  mission_start_date: Date;
  mission_end_date: Date;
  active: boolean;
  promo_point_code: string;
  promo_coupon_type?: any;
  for_new_user_only: string;
  promo_img_url: string;
  mission_icon: string;
  auto_attend?: any;
  mission_reward: string;
  mission_reward_text: string;
  in_period: string;
  order_no: number;
  mission_step_type: string;
  in_period_per_day?: any;
  is_received: string;
  step_amount: number;
  step_current: number;
  mission_step_command: string;
  max_points: number;
  end_date: Date;
  activity: string;
  shop_id: number;
  mission_description: string;
}

export interface IMissionDetail {
  mission_id: string;
  mission_code: string[];
  mission_name: string;
  mission_desc: string;
  limit_promo_per_giver: number;
  limit_promo_per_receiver: number;
  limit_promo_per_mission: number;
  mission_start_date: Date;
  mission_end_date: Date;
  active: boolean;
  promo_point_code: string;
  promo_coupon_type?: any;
  for_new_user_only?: any;
  promo_img_url?: any;
  mission_icon: string;
  auto_attend?: any;
  mission_reward: string;
  in_period: string;
  order_no: number;
  mission_step_type: string;
  in_period_per_day?: any;
  is_received: string;
  step_amount: number;
  step_current: number;
  mission_step_name: string;
  mission_step_id: string;
  mission_step_complete: string;
  mission_step_command: string;
  mission_step_url: string;
  max_points: number;
  mission_config_info_id: string;
  header: string;
  mission_step: string;
  mission_reward_text: string;
  mission_reward_icon: string;
  mission_condition: string;
  mission_button_text: string;
  mission_button_link: string;
  name: string;
  mission_image: string;
  mission_description: string;
  shop_id: string;
  activity: string;
}
